pub const GB = .{
    .CLOCK_RATE = 4194304,
};

pub const AUDIO = .{
    .SAMPLE_RATE = 48000,
    .NUM_CHANNELS = 2,
    .SAMPLES_BUFFER_LEN = 512,
};

pub const DEBUG = .{
    .DISPLAY_FPS = true,
    .OUTPUT_AUDIO_FILES = true,
};
