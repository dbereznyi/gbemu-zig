pub const GB = .{
    .CLOCK_RATE = 4194304,
    .SCREEN_WIDTH = 160,
    .SCREEN_HEIGHT = 144,
};

pub const AUDIO = .{
    .SAMPLE_RATE = 48000,
    .NUM_CHANNELS = 2,
    .SAMPLES_BUFFER_LEN = 512,
};

pub const DEBUG = .{
    .DISPLAY_FPS = true,
    .OUTPUT_AUDIO_FILES = false,
};
